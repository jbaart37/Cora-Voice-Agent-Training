# =================================================================
# CORA Voice Agent - Configuration Template
# =================================================================
# Copy this file to .env and fill in YOUR actual values
# Command: cp .env.example .env (Linux/Mac) or copy .env.example .env (Windows)
# =================================================================

# ─────────────────────────────────────────────────────────────────
# Azure Account Information (from Module 1, Step 3)
# ─────────────────────────────────────────────────────────────────
# Your Azure subscription ID (get from: az account show)
AZURE_SUBSCRIPTION_ID=12345678-1234-1234-1234-123456789012

# Your preferred Azure region (recommended: eastus or eastus2)
AZURE_LOCATION=eastus

# Your environment name - used by azd for tracking deployments
# Keep it simple: dev, test, prod
AZURE_ENV_NAME=dev

# ─────────────────────────────────────────────────────────────────
# Resource Group (from Module 1, Step 5)
# ─────────────────────────────────────────────────────────────────
# Your resource group name (will be created during deployment)
# Pattern: rg-cora-{AZURE_ENV_NAME}
# Example: if AZURE_ENV_NAME=dev, use rg-cora-dev
RESOURCE_GROUP=rg-cora-dev

# ─────────────────────────────────────────────────────────────────
# Azure AI Foundry Configuration (from Module 1, Step 6)
# ─────────────────────────────────────────────────────────────────
# Your AI Foundry project endpoint URL
# Format: https://your-project-name.openai.azure.com/
AZURE_AI_FOUNDRY_ENDPOINT=https://your-project-name.openai.azure.com/

# Your GPT model deployment name (e.g., gpt-4o, gpt-4o-mini)
AZURE_AI_MODEL_NAME=gpt-4o

# Optional: API Key (only needed for local development)
# When deploying to Azure, Managed Identity is used automatically (more secure)
# Leave this commented out unless testing locally
# AZURE_AI_FOUNDRY_API_KEY=your-api-key-here

# ─────────────────────────────────────────────────────────────────
# Flask Configuration (for local development only)
# ─────────────────────────────────────────────────────────────────
# Flask environment (development or production)
FLASK_ENV=development

# Flask port (default: 5000)
FLASK_PORT=5000

# Flask secret key (generate with: python -c "import secrets; print(secrets.token_hex(32))")
# CHANGE THIS IN PRODUCTION!
FLASK_SECRET_KEY=dev-secret-key-change-in-production

# ─────────────────────────────────────────────────────────────────
# Application Insights (auto-populated by azd after deployment)
# ─────────────────────────────────────────────────────────────────
# Leave empty - Azure will set this during deployment
# APPLICATIONINSIGHTS_CONNECTION_STRING=

# ─────────────────────────────────────────────────────────────────
# Azure Storage (auto-populated by azd after deployment)
# ─────────────────────────────────────────────────────────────────
# Leave empty - Azure will set this during deployment
# AZURE_STORAGE_ACCOUNT_NAME=

# =================================================================
# SECURITY NOTES
# =================================================================
# ✅ This .env.example file is safe to commit (no real secrets)
# ✅ Your actual .env file is in .gitignore (won't be committed)
# ⚠️  NEVER commit your .env file with real credentials to Git
# ⚠️  For production deployments, use Azure Managed Identity (no API keys needed)
# =================================================================
