# Azure Developer CLI Configuration
name: voice-agent-simulator
metadata:
  template: voice-agent-simulator@1.1.0

services:
  web:
    language: python
    project: ./src
    host: containerapp
    docker:
      path: ./src/Dockerfile
      context: ./src
